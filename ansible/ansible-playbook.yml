---
- name: Deploy Speech-to-Text Application to VM
  hosts: terraform_vms
  become: true
  vars:
    project_root: "/home/vboxuser/Documents/speech-to-text"
    destination_path: "/opt/speech-to-text"


  tasks:
    - name: Install required packages
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
        state: present
        update_cache: yes
      become: true

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present
      become: true

    - name: Add Docker repository
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present
        update_cache: yes
      become: true

    - name: Install Docker Engine
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: yes
      become: true

    - name: Ensure Docker service is running
      service:
        name: docker
        state: started
        enabled: true
      become: true

    - name: Add user to docker group
      user:
        name: azureuser
        groups: docker
        append: yes
      become: true
    - name: Ensure Docker is installed
      package:
        name:
          - docker
        state: present
      become: true

    - name: Ensure Docker service is running
      service:
        name: docker
        state: started
        enabled: true
      become: true

    - name: Create destination directory
      file:
        path: "{{ destination_path }}"
        state: directory
        mode: '0755'
      become: true
      
    - name: Change ownership of destination directory
      file:
        path: "{{ destination_path }}"
        state: directory
        owner: azureuser
        group: azureuser
        recurse: yes
      become: true

    - name: Sync VM directory to target machine
      synchronize:
        src: "{{ project_root }}/vm/"
        dest: "{{ destination_path }}"
        rsync_opts:
          - "--exclude=node_modules"
          - "--exclude=.git"
      delegate_to: localhost
      become: false

    - name: Run VM docker-compose with frontend and backend
      command:
        cmd: docker compose up -d --force-recreate
        chdir: "{{ destination_path }}"
      become: true



    - name: Create Grafana directory
      file:
        path: "/opt/grafana"
        state: directory
        mode: '0755'
        owner: azureuser
        group: azureuser
      become: true

    - name: Create Prometheus directory
      file:
        path: "/opt/prometheus"
        state: directory
        mode: '0755'
        owner: azureuser
        group: azureuser
      become: true

    - name: Sync Grafana provisioning directory
      synchronize:
        src: "{{ project_root }}/grafana/"
        dest: "/opt/grafana"
        rsync_opts:
          - "--exclude=.git"
      delegate_to: localhost
      become: false

    - name: Create Prometheus configuration
      template:
        src: "{{ project_root }}/grafana/prometheus.yml.j2"
        dest: "/opt/prometheus/prometheus.yml"
        mode: '0644'
      become: true

    - name: Create docker-compose for monitoring
      template:
        src: "{{ project_root }}/grafana/docker-compose-monitoring.yml"
        dest: "/opt/grafana/docker-compose.yml"
        mode: '0644'
      become: true

    - name: Start Grafana and Prometheus containers
      command:
        cmd: docker compose up -d --force-recreate
        chdir: "/opt/grafana"
      become: true